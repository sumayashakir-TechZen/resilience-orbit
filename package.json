{
  "name": "resilienceorbit",
  "private": true,
  "scripts": {
    "build": "ELEVENTY_ENV=production npx @11ty/eleventy",
    "dev": "npx @11ty/eleventy --serve --port=8080"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.0",
    "@11ty/eleventy-plugin-sitemap": "^5.0.0",
    "markdown-it": "^14.1.0"
  }
}